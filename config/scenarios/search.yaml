name: search_and_navigate
goal: >
  Search for the Playwright Python documentation and open the
  official playwright.dev site.
start_url: https://google.com
policy:
  domain_allowlist: [google.com, playwright.dev]
budgets:
  max_steps: 20
secrets: {}